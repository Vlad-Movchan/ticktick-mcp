services:
  productivity-mcp:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      TICKTICK_TOKEN_PATH: /data/tokens.json
    volumes:
      - ticktick-tokens:/data

volumes:
  ticktick-tokens:
